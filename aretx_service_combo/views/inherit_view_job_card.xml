<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="inherited_job_card_line" model="ir.ui.view">
        <field name="name">job.card.inherited</field>
        <field name="model">job.card</field>
        <field name="inherit_id" ref="aretx_job_card.view_job_card_form"/>
        <field name="arch" type="xml">
            <xpath expr="/form/sheet/notebook/page/field[@name='x_job_card_ids']/tree/field[@name='price']"
                   position="after">

                <!--                <field name="check" invisible="True"/>-->
                <!--                <field name="vehicle_id" invisible="True"/>-->
                <field name="expiry_date" optional="hide"/>


            </xpath>
        </field>
    </record>

</odoo>